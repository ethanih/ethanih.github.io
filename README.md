# ethanih.github.io

Personal website for GUO Yiheng.

The site is intentionally small: an about page, a notes page, one stylesheet, and course-note assets under `assets/lecture_note/`.

## Local build

```bash
gem install jekyll
jekyll build
jekyll serve
```
